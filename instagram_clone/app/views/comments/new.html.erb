<div class='form'>
  <%= form_for [@post, @comment] do |f| %>
    <div class="field">
      <%= f.label :message %>
        <br>
      <%= f.text_area :message %>
    </div>
    <%= f.submit 'Leave Comment' %>
  <% end %>
</div>
